doctype html
html
  head
    title Welcome to nanzi – Your New Home Awaits!
    style.
      @import url('https://fonts.googleapis.com/css2?family=MuseoModerno:wght@600&display=swap');

      body {
        margin: 0;
        padding: 0;
        background: #0F0F0F;
        color: #f5f5f5;
        font-family: 'MuseoModerno', Arial, sans-serif;
      }

      /* Header */
      .header {
        background: linear-gradient(90deg, #31204a, #141414, #0F0F0F);
        padding: 30px;
        text-align: center;
      }
      .logo {
        font-family: 'MuseoModerno', Arial, sans-serif;
        font-size: 48px;
        color: #fff;
        letter-spacing: 2px;
        text-shadow: 0 0 10px rgba(168, 85, 247, 0.8);
      }
      .logo span {
        color: #a855f7;
      }
      .tagline {
        font-size: 16px;
        color: #bbb;
        margin-top: 5px;
      }

      /* Hero Section */
      .hero {
        text-align: center;
        padding: 40px 20px 20px;
      }
      .headline {
        font-size: 28px;
        font-weight: bold;
        text-shadow: 0px 0px 8px rgba(168, 85, 247, 0.6);
      }
      .subtext {
        font-size: 16px;
        color: #5a5a5aff;
        margin-top: 10px;
        max-width: 500px;
        margin-left: auto;
        margin-right: auto;
      }

      /* Features */
      .features {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        margin: 30px auto;
        max-width: 600px;
      }
      .feature {
        background: rgba(0, 0, 0, 0.85);
        border: 1px solid rgba(168, 85, 247, 0.2);
        border-radius: 12px;
        padding: 15px;
        margin: 10px;
        flex: 1 1 150px;
        text-align: center;
        box-shadow: 0 4px 20px rgba(133, 133, 133, 0.4);
      }
      .feature-title {
        font-weight: bold;
        color: #a855f7;
        margin-bottom: 5px;
      }
      .feature-text {
        font-size: 14px;
        color: #ddd;
      }

      /* Tenancy Card */
      .card {
        margin: 40px auto;
        padding: 25px;
        max-width: 500px;
        background: linear-gradient(145deg, rgba(31,31,31,0.95), rgba(45,35,65,0.95));
        border-radius: 14px;
        box-shadow: 0 8px 30px rgba(0,0,0,0.5);
        border: 1px solid rgba(168, 85, 247, 0.2);
        text-align: center;
      }
      .date-title {
        font-size: 18px;
        margin-bottom: 8px;
        color: #ccc;
      }
      .date-value {
        font-size: 22px;
        font-weight: bold;
        color: #a855f7;
        text-shadow: 0 0 8px rgba(168,85,247,0.6);
      }

      /* Button */
      .button {
        display: inline-block;
        margin-top: 20px;
        padding: 14px 36px;
        background: linear-gradient(270deg, #a855f7, #7e22ce, #a855f7);
        background-size: 400% 400%;
        animation: gradientShift 6s ease infinite;
        color: #ffffffff;
        text-decoration: none;
        font-weight: bold;
        border-radius: 50px;
        box-shadow: 0 6px 20px rgba(168, 85, 247, 0.5);
        text-shadow: 0px 1px 3px rgba(0,0,0,0.3);
        font-size: 16px;
      }

      .button:link,
      .button:visited,
      .button:hover,
      .button:active {
        color: #fff !important;
       text-decoration: none !important;
      }

      @keyframes gradientShift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }

      /* Footer */
      .footer {
        text-align: center;
        font-size: 12px;
        color: #777;
        padding: 20px;
        border-top: 1px solid rgba(255,255,255,0.1);
        margin-top: 30px;
      }
      .footer a {
        color: #a855f7;
        text-decoration: none;
      }

  body
    //- Header
    .header
      div.logo
        span n
        | anzi
      div.tagline Your modern living experience starts here

    //- Hero
    .hero
      div.headline Welcome to Your New Home
      div.subtext Hello, you’re just one step away from joining the nanzi community. We can’t wait to see you settle in and enjoy a premium living experience.

    //- Features
    .features
      .feature
        div.feature-title Prime Location
        div.feature-text Live close to everything you need while enjoying the peace of a premium neighborhood.
      .feature
        div.feature-title Modern Comfort
        div.feature-text Fully equipped, stylish spaces designed for contemporary living.
      .feature
        div.feature-title Community
        div.feature-text Be part of a vibrant community with exclusive resident benefits.

    //- Tenancy Card
    .card
      div.date-title Your tenancy starts on
      div.date-value #{startDate}
      a.button(href=`${baseUrl}/tenant-accept-invitation?token=${token}`) Accept Invitation

    //- Footer
    .footer
      p Need help? Contact us at 
        a(href=`mailto:${supportEmail}`) nanzi.info@gmail.com
      p Sent by nanzi. All rights reserved.
      
